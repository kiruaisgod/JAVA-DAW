public class Tarea01UD2ForMario {
    public static void main(String[] args) {

	int num;
    
	    // Ejercicio 1. 
        for (num = 125; num >= 0; num -= 5) {
            System.out.println(num);
        }

        // Ejercicio 2. 
        for (num = 1415; num >= 0; num -= 5) {
            System.out.println(num);
        }

        // Ejercicio 3. 
        for (num = 10500; num >= 0; num -= 5) {
            System.out.println(num);
        }

        // Ejercicio 4. 
        for (num = 2600; num >= 100; num -= 10) {
            System.out.println(num);
        }

        // Ejercicio 5. 
        for (num = 14; num <= 104; num += 4) {
            System.out.println(num);
        }

        // Ejercicio 6. 
        for (num = 3; num <= 1005; num += 3) {
            System.out.println(num);
        }

        // Ejercicio 7. 
        for (num = 0; num <= 40; num += 10) {
            System.out.println(num);
        }

        // Ejercicio 8. 
        for (num = 100; num >= 0; num -= 10) {
            System.out.println(num);
        }

        // Ejercicio 9.
        for (num = 100; num >= 20; num -= 10) {
            System.out.println(num);
        }

        // Ejercicio 10.
        for (num = 10000; num >= 200; num -= 100) {
            System.out.println(num);
        }
    }
}
